# Atelierr Docker 环境变量（复制为 docker/.env 后修改）
#
# 2026-09-18 用户裁决：Flatnotes 网页版退役，相关 FLATNOTES_* 与
# MEMORY_DATA_DIR 变量已随服务一并移除（旧 .env 里残留不影响运行）。

# 容器文件属主（与宿主机用户一致，避免权限问题）
PUID=1000
PGID=1000

# $OV 数据树根（Syncthing 容器挂载点，如 /home/<you>/atelierr-data）
OV_DATA_DIR=
