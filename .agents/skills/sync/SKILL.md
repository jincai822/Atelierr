---
name: sync
description: "Run the Atelier `/sync` workflow in Codex using the shared command registry. Use when the user explicitly invokes `$sync`. Digest the mobile-capture submodule into the persistent L2 destinations and stage zettelm deletions."
---

<!-- GENERATED by scripts/render_runtime_edges.py from harness/commands.toml; edit the registry and re-render. -->

## Atelier Command

Run the explicit Codex `$sync` skill. Its authoritative workflow source is
the Claude Code `/sync` command specification.

1. Read `AGENTS.md` and `CLAUDE.md`.
2. Read the authoritative `.claude/commands/sync.md` command specification directly.
3. Execute it in this thread using the Codex adaptation table in `AGENTS.md`.
4. Treat text following `$sync` as command context or arguments.
5. Do not start a nested Codex process; complete the workflow in this thread.
