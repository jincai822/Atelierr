# GENERATED by scripts/atelier/render_runtime_edges.py; edit harness/commands.toml and re-render.
interface:
  display_name: "Atelier $sync"
  short_description: "Run the Atelier $sync skill in Codex"
  default_prompt: "Use $sync to run the matching Atelier command."

policy:
  allow_implicit_invocation: false
