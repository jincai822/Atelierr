interface:
  display_name: "Atelier $civ"
  short_description: "Run the Atelier $civ skill in Codex"
  default_prompt: "Use $civ to run the matching Atelier command."

policy:
  allow_implicit_invocation: false
