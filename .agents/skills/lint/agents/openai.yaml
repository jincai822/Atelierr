interface:
  display_name: "Atelier $lint"
  short_description: "Run the Atelier $lint skill in Codex"
  default_prompt: "Use $lint to run the matching Atelier command."

policy:
  allow_implicit_invocation: false
