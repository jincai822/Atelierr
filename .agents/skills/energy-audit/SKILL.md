---
name: energy-audit
description: "Run the Atelier `/energy-audit` workflow in Codex using the shared command registry. Use when the user explicitly invokes `$energy-audit`. Assessment of physical, mental, emotional, and social energy patterns."
---

## Atelier Command

Run the explicit Codex `$energy-audit` skill. Its authoritative workflow source is
the Claude Code `/energy-audit` command specification.

1. Read `AGENTS.md` and `CLAUDE.md`.
2. Read the authoritative `.claude/commands/energy-audit.md` command specification directly.
3. Execute it in this thread using the Codex adaptation table in `AGENTS.md`.
4. Treat text following `$energy-audit` as command context or arguments.
5. Do not start a nested Codex process; complete the workflow in this thread.
