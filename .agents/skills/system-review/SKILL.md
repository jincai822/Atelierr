---
name: system-review
description: "Run the Atelier `/system-review` workflow in Codex using the shared command registry. Use when the user explicitly invokes `$system-review`. Review system-evolution changes with internal and external reviewers."
---

<!-- GENERATED by scripts/atelier/render_runtime_edges.py from harness/commands.toml; edit the registry and re-render. -->

## Atelier Command

Run the explicit Codex `$system-review` skill. Its authoritative workflow source is
the Claude Code `/system-review` command specification.

1. Read `AGENTS.md` and `CLAUDE.md`.
2. Read the authoritative `.claude/commands/system-review.md` command specification directly.
3. Execute it in this thread using the Codex adaptation table in `AGENTS.md`.
4. Treat text following `$system-review` as command context or arguments.
5. Do not start a nested Codex process; complete the workflow in this thread.
